# PostgreSQL — shared server, schemas `bricksmith` and `bricksmith_rag` will be
# created inside the target database.
DB_URL=postgresql://user:pass@host:5432/dbname

# LLM provider: "xai" (Grok) or "openai"
MODEL_PROVIDER=xai

# xAI Grok (used when MODEL_PROVIDER=xai)
XAI_API_KEY=xai-...
XAI_BASE_URL=https://api.x.ai/v1
XAI_MODEL=grok-4-fast-reasoning
XAI_AGENT_MODEL=grok-4

# OpenAI (used when MODEL_PROVIDER=openai)
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4.1
OPENAI_AGENT_MODEL=gpt-4.1

# Embeddings. Default is `local` (fastembed, no API key, ~100 MB ONNX model).
# Switch to `openai` if you have OPENAI_API_KEY and want higher-quality vectors.
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
EMBEDDING_DIM=384

# App
APP_ENV=dev
APP_SECRET=change-me
PORT=5057
